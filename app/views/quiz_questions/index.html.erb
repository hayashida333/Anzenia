<h1>クイズ一覧</h1>

<div class="mb-4">
  <div class="d-flex gap-2">
    <%= button_to "記事一覧", posts_path, method: :get, class: "btn btn-outline-primary" %>
    <%= button_to "動画一覧", videos_path, method: :get, class: "btn btn-outline-success" %>
    <%= button_to "クイズ一覧", quiz_questions_path, method: :get, class: "btn btn-warning" %>
  </div>
</div>

<h2>クイズには何度でも挑戦できますが、1回目の回答が理解度として報告されます。</h2>


<% if @quiz_questions.present? %>
  <table class="table table-striped table-bordered align-middle mt-3">
    <thead>
      <tr>
        <th style="width: 50px;">ID</th>
        <th>クイズ</th>
        <th style="width: 200px;">リクエスト</th>
      </tr>
    </thead>
    <tbody>
      <% @quiz_questions.each do |quiz_question| %>
        <tr>
          <td><%= quiz_question.id %></td>
          <td>
            <%= link_to quiz_question.question, quiz_question_path(quiz_question) %>
          </td>
          <td>
            <% if quiz_question.requested_user.present? %>
              <span class="text-success fw-bold"><%= quiz_question.requested_user.full_name %></span>
            <% else %>
              <span class="text-muted">未設定</span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="alert alert-info">クイズはまだ登録されていません。</div>
<% end %>

<%= paginate @quiz_questions %>