<% content_for :title, "投稿の編集" %>

<h1>投稿の編集</h1>

<%= form_with(model: [:admin, @video], local: true) do |form| %>
  <%= render "form", form: form %>
<% end %>

<br>

<div>
  <%= link_to "この投稿を表示", admin_video_path(@video) %> 
  <%= link_to "投稿一覧", admin_videos_path %>
</div>
