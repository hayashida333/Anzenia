<% content_for :title, "Editing post" %>

<h1>編集画面</h1>

<%= form_with(model: @post, url: admin_post_path(@post), local: true, method: :patch) do |form| %>
  <%= render "form", form: form %> <!-- ✅ form を明示的に渡す -->
<% end %>

<div class="mt-3">
  <%= link_to "一覧へ戻る", admin_posts_path %>
</div>
