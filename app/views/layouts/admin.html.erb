<!DOCTYPE html>
<html class="h-100">
  <head>
    <title>情報共有システム - 管理画面</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100 bg-light">
    <main class="flex-grow-1">
      <div class="container-fluid">
        <div class="row">
          <!-- ✅ サイドバー -->
          <nav class="col-md-3 col-lg-2 d-md-block bg-dark sidebar text-white min-vh-100 p-3">
            <div class="list-group">
              <%= link_to "従業員一覧", admin_users_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <%= link_to "記事一覧（Post）", admin_posts_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <%= link_to "動画一覧（Video）", admin_videos_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <%= link_to "クイズ一覧（Quiz）", admin_quiz_questions_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <%= link_to "クイズ結果（Quiz）", admin_quiz_results_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <%= link_to "機能確認", posts_path, class: "list-group-item list-group-item-action bg-dark text-white border-0" %>
              <hr class="border-secondary">
              <% if user_signed_in? %>
                <div class="list-group-item bg-dark text-white border-0 disabled">
                  ログイン中: <%= current_user.email %>
                </div>
                <%= button_to "ログアウト", destroy_user_session_path, method: :delete,
                      class: "list-group-item list-group-item-action bg-dark text-white border-0 text-start w-100" %>
              <% end %>
            </div>
          </nav>

          <!-- ✅ メインコンテンツ -->
          <div class="col-md-9 col-lg-10 p-4">
            <% flash.each do |key, message| %>
              <div class="alert alert-<%= key == "notice" ? "success" : "danger" %>">
                <%= message %>
              </div>
            <% end %>

            <%= yield %>
          </div>
        </div>
      </div>
    </main>

    <footer class="footer mt-auto py-3 bg-white border-top text-center">
      <div class="container">
        <span class="text-muted">情報共有システム（管理画面）</span>
      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
